============
Installation
============

At the command line::

    $ easy_install drf-file-management

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv drf-file-management
    $ pip install drf-file-management
